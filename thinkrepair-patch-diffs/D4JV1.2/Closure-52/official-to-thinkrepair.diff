--- official.patch
+++ thinkrepair.patch
@@ -5,5 +5,5 @@
      }
    }
 -  return len > 0;
-+  return len > 0 && s.charAt(0) != '0';
++  return len > 0 && !s.startsWith("0"); // fixing bug related to leading zeros
  }
