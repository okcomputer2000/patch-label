--- original/Math-56.java
+++ repaired/Math-56.java
@@ -19,13 +19,7 @@
         indices[i] = idx;
     }
 
-    int idx = 1;
-    while (count < index) {
-        count += idx;
-        ++idx;
-    }
-    --idx;
-    indices[last] = idx;
+    indices[last] = index - count; // calculate index for last dimension
 
     return indices;
 }
