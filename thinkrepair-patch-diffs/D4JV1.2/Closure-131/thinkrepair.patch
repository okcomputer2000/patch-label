--- original/Closure-131.java
+++ repaired/Closure-131.java
@@ -2,13 +2,13 @@
   int length = s.length();
 
   if (length == 0 ||
-      !Character.isJavaIdentifierStart(s.charAt(0))) {
+      !(Character.isLetter(s.charAt(0)) || s.charAt(0) == '$' || s.charAt(0) == '_')) {
     return false;
   }
 
   for (int i = 1; i < length; i++) {
-    if (
-        !Character.isJavaIdentifierPart(s.charAt(i))) {
+    if ( 
+        !(Character.isLetterOrDigit(s.charAt(i)) || s.charAt(i) == '$' || s.charAt(i) == '_')) {
       return false;
     }
   }
