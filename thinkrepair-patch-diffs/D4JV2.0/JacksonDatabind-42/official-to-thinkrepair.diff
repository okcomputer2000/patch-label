--- official.patch
+++ thinkrepair.patch
@@ -4,8 +4,9 @@
          return URI.create("");
      }
      // As per [databind#1123], Locale too
+-    return super._deserializeFromEmptyString();
 +    if (_kind == STD_LOCALE) {
 +        return Locale.ROOT;
 +    }
-     return super._deserializeFromEmptyString();
++    return super._deserializeFromEmptyString(); 
  }
