--- original/Jsoup-51.java
+++ repaired/Jsoup-51.java
@@ -2,5 +2,5 @@
     if (isEmpty())
         return false;
     char c = input[pos];
-    return (c >= 'A' && c <= 'Z') || (c >= 'a' && c <= 'z');
+    return Character.isLetter(c); // Use built-in method to check if char is a letter
 }
