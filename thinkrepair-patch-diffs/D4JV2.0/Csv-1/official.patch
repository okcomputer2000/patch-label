--- original/Csv-1.java
+++ repaired/Csv-1.java
@@ -1,6 +1,6 @@
 public int read() throws IOException {
     int current = super.read();
-    if (current == '\n') {
+    if (current == '\r' || (current == '\n' && lastChar != '\r')) {
         lineCounter++;
     }
     lastChar = current;
