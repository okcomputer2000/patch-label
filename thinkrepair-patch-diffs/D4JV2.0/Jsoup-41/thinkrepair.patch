--- original/Jsoup-41.java
+++ repaired/Jsoup-41.java
@@ -5,5 +5,9 @@
 
     Element element = (Element) o;
 
-    return this == o;
+    if (!this.tag().equals(element.tag())) { // compare tags as well
+        return false;
+    }
+
+    return Objects.equals(this.attributes(), element.attributes()); // compare attribute maps
 }
