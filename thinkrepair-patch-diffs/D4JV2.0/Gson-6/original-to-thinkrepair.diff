--- original/Gson-6.java
+++ thinkrepair/Gson-6.java
@@ -14,6 +14,8 @@
     throw new IllegalArgumentException(
         "@JsonAdapter value must be TypeAdapter or TypeAdapterFactory reference.");
   }
+  if (typeAdapter != null) {
     typeAdapter = typeAdapter.nullSafe();
+  }
   return typeAdapter;
 }
