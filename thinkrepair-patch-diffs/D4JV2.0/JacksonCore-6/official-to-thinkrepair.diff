--- official.patch
+++ thinkrepair.patch
@@ -1,20 +1,12 @@
 --- original/JacksonCore-6.java
 +++ repaired/JacksonCore-6.java
-@@ -6,8 +6,15 @@
+@@ -6,6 +6,9 @@
          return -1;
      }
      // [core#176]: no leading zeroes allowed
--    for (int i = 0; i < len; ++i) {
--        char c = str.charAt(i);
-+    char c = str.charAt(0);
-+    if (c <= '0') {
-+        return (len == 1 && c == '0') ? 0 : -1;
-+    }
-+    if (c > '9') {
++    if (len > 1 && str.charAt(0) == '0') {
 +        return -1;
 +    }
-+    for (int i = 1; i < len; ++i) {
-+        c = str.charAt(i);
+     for (int i = 0; i < len; ++i) {
+         char c = str.charAt(i);
          if (c > '9' || c < '0') {
-             return -1;
-         }
